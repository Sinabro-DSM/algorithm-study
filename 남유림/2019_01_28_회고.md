~~~js
function solution(numbers) {
    let answer = '';
    let temp = 0;

    
    for (let i=0; i<numbers.length; i++) {
        if(`${num[i]}` + `${num[i+1]}` < `${num[i+1]} + `${num[i]}`) {
            temp = num[i];
            num[i] = num[i+1];
            num[i+1] = temp;
        }
    }
    
    return answer;
}
~~~